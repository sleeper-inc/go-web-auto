rod:
  headless: false  #Run browser invisibly
  timeout: 300 #Global timeout time

browser:
  browser_path: "" #Custom Chrome path
  viewport: "1920,1080" #Window size (width,height)

network:
  throttle: "4G" #Network configuration

